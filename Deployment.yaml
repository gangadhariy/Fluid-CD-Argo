apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-api
  namespace: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-api
  template:
    metadata:
      labels:
        app: python-api
    spec:
      containers:
      - name: api
        image: gangadhariy/fluid-ai-app:11
        ports:
        - containerPort: 5000
        env:
        - name: DB_HOST
          value: postgres
        envFrom:
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            cpu: "4"
            memory: "4Gi"
          limits:
            cpu: "5"
            memory: "5Gi"
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 20
